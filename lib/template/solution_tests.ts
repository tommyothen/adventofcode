import { expect, test } from "bun:test";
import Solution from "./solution";

test("YYYY/DD |> Part 1", async () => {
  const input = ``;

  expect(await Solution.part1(input)).toBe(0);
});

test("YYYY/DD |> Part 2", async () => {
  const input = ``;

  expect(await Solution.part2(input)).toBe(0);
});
